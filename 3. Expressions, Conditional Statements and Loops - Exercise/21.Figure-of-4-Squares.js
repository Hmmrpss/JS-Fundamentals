function drawFigure(size){
  let case = '';
  if (size % 2 == 0) {
    case = 'even';
  } else {
    case == 'odd';
  }

  switch (case) {
    case 'odd':

      break;
    case 'even':

    break;
    default: console.log("error");
  }

  
}

drawFigure(4);
